<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.crq.mybatis.mapper.StudentMapper">

    <resultMap id="studentMap" type="Student">
            <id property="studentId" column="student_id" jdbcType="INTEGER"/>
            <result property="clazzId" column="clazz_id" jdbcType="INTEGER"/>
            <result property="studentName" column="student_name" jdbcType="VARCHAR"/>
            <result property="hometown" column="hometown" jdbcType="VARCHAR"/>
            <result property="birthday" column="birthday" jdbcType="DATE"/>
        <association property="clazz" javaType="Clazz">
            <id column="clazz_id" property="clazzId"/>
            <result column="clazz_name" property="clazzName"/>
        </association>
        <collection property="courses" ofType="Course">
            <id column="course_id" property="courseId"/>
            <result column="course_name" property="courseName"/>
        </collection>
    </resultMap>


    <select id="findManyByOne" resultType="Student"></select>
    <select id="getStudent" resultType="Student">
        SELECT s.student_id,student_name,s.hometown,s.birthday,c.clazz_id,c.clazz_id,
               c.clazz_name,cc.course_id,cc.course_name
        FROM t_student s
                 LEFT JOIN t_clazz c
                            ON s.clazz_id = c.clazz_id
                 LEFT JOIN t_course_student ts
                           ON s.student_id = ts.student_id
                 LEFT JOIN t_course cc
                           ON ts.course_id = cc.course_id
        WHERE s.student_id = #{studentId}
    </select>
</mapper>
